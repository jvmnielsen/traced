#pragma once
#include <vector>
#include <memory>
//#include "src/Shape.h"
//#include "src/Mesh.h"

/*
class BVH
{
public:
    BVH(std::vector<std::unique_ptr<Mesh>> shapes);

    ~BVH();


    std::vector<std::unique_ptr<Mesh>> m_shapes;
}; */

